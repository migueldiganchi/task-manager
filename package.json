{
  "name": "fasobook-app",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "moment": "^2.29.1",
    "node-sass": "^4.14.1",
    "npm-run-all": "^4.1.5",
    "react": "^17.0.2",
    "react-datepicker": "^4.7.0",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1",
    "sass-loader": "^7.1.0",
    "serve": "^11.1.0"
  },
  "scripts": {
    "dev": "set PORT=3303 && react-scripts start",
    "server": "npx json-server --watch data/db.json --port 9001",
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
